version: '3'
services:
  api-graphql:
    image: gustavosilvasouza/sistema-gerenciamento-usuarios:v1
    container_name: sistema-gerenciamento-usuarios
    restart: always
    ports: 
      - 5000:3000
    networks:
      - api-graphql-network
networks:
  api-graphql-network:
    driver: bridge